// @flow
import type {
  UserGroupConfig,
} from "../types"

export type UserGroup = {}

export type UserGroups = { [string]: UserGroup }

// TODO: do UserGroup
export const constructUserGroup = (userGroup: UserGroupConfig): UserGroup => ({})