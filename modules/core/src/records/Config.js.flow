// @flow strict
import type { TestConfig, InputTestConfig } from "./TestConfig";

export interface InputConfig {
  tests: { [id: string]: InputTestConfig };
}

export interface Config {
  tests: { [id: string]: TestConfig };
}

declare export function getTestsConfig(configTests: {
  [id: string]: InputTestConfig
}): { [id: string]: TestConfig };

declare export function getConfig(inputConfig: InputConfig): Config;
